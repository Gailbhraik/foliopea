export const fmt = v => new Intl.NumberFormat('fr-FR',{style:'currency',currency:'EUR',maximumFractionDigits:2}).format(v);
export function enrich(rows){ return rows.map(r=>{ const value=r.qty*r.price, cost=r.qty*r.pru, perf=value-cost, buyQty=r.buys?.reduce((s,b)=>s+b.qty,0)||0, buyCost=r.buys?.reduce((s,b)=>s+b.qty*b.price,0)||0; return { ...r, value, cost, perf, perfPct: cost ? perf/cost*100 : 0, avgBuy: buyQty ? buyCost/buyQty : r.pru }; }); }
export function summarize(rows){ const computed=enrich(rows); const total=computed.reduce((s,r)=>s+r.value,0); const cost=computed.reduce((s,r)=>s+r.cost,0); const perf=total-cost; const bySector=computed.reduce((a,r)=>(a[r.sector]=(a[r.sector]||0)+r.value,a),{}); return { computed, total, cost, perf, cash: 1260, bySector }; }
